import React from "react";
import "./home.css";

const categories = ["Electronics", "Fashion", "Groceries", "Mobiles", "Home"];
const products = [
  { id: 1, name: "Smartphone", price: "$299", image: "https://via.placeholder.com/150" },
  { id: 2, name: "Shoes", price: "$99", image: "https://via.placeholder.com/150" },
  { id: 3, name: "Laptop", price: "$799", image: "https://via.placeholder.com/150" },
  { id: 4, name: "Watch", price: "$199", image: "https://via.placeholder.com/150" },
  { id: 5, name: "Headphones", price: "$49", image: "https://via.placeholder.com/150" },
];

function HomePage() {
  return (
    <div className="home-page">
      {/* Hero Banner */}
      <section className="hero-banner">
        <div className="hero-content">
          <h1>Welcome to Our Store!</h1>
          <p>Best deals everyday just for you!</p>
          <button>Shop Now</button>
        </div>
      </section>

      {/* Categories */}
      <section className="categories">
        <h2>Categories</h2>
        <div className="category-buttons">
          {categories.map((cat, i) => (
            <button key={i}>{cat}</button>
          ))}
        </div>
      </section>

      {/* Products Carousel/Grid */}
      <section className="products">
        <h2>Hot Deals</h2>
        <div className="product-grid">
          {products.map((p) => (
            <div className="product-card" key={p.id}>
              <img src={p.image} alt={p.name} />
              <h3>{p.name}</h3>
              <p>{p.price}</p>
              <button>Add to Cart</button>
            </div>
          ))}
        </div>
      </section>

      {/* Promo Section */}
      <section className="promo">
        <h2>Special Offers</h2>
        <div className="promo-banner">
          <p>Upto 50% off on selected items!</p>
        </div>
      </section>
    </div>
  );
}

export default HomePage;
